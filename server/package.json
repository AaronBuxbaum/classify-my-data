{
  "dependencies": {
    "graphql-tag": "^2.10.1",
    "graphql-yoga": "^1.17.4",
    "mongoose": "^5.7.5"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-stage-2": "^6.24.1",
    "nodemon": "^1.18.10"
  },
  "scripts": {
    "test": "npm run test",
    "dev": "nodemon -e graphql,js --exec babel-node src/index.js",
    "build": "NODE_ENV=PRODUCTION babel src -d dist --copy-files",
    "serve": "NODE_ENV=production node dist/index.js"
  }
}
