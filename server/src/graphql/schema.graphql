type Vote {
  id: String
  vote: String
  createdOn: String
}

input VoteInput {
  id: String
  vote: String
  username: String
}

type Query {
  votes: [Vote!]!
}

type Mutation {
  createVotes(votes: [VoteInput]): Boolean!
}
